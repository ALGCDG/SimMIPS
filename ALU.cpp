#include"ALU.hpp"

int execute(unsigned int instruction)
{
    // bool is_j_type, is_r_type;
    // char rs, rt, rd;
    // int immediate, address;
    // // validate is right length
    // // get control values
    // char ALUOp;
    // // get arguments
    // int operand1 = !(is_j_type) ? read_register(rt) : address; // register operand (rt) or address (for J)
    // int operand2 = (is_r_type) ? read_register(rd) : immediate; // either contentse of register rd, or immediate
    // // apply ALU operation
    // int result = ALU(ALUOp, operand1, operand2);
    // // apply output
    // if (RegWrite)
    // else if (MemWrite)
    // else if (PCSrc)
    // else if (MemtoReg)
    // // return exit state
    // return 0;
}
